import { Text, View } from "react-native";

export default function ProfileScreen() {
  // Dummy screen placeholder (requested): replace with real profile UI later.
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Profile</Text>
    </View>
  );
}

