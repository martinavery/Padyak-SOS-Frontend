import { Text, View } from "react-native";

export default function SyncScreen() {
  // Dummy screen placeholder (requested): replace with real sync UI later.
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>Sync</Text>
    </View>
  );
}

